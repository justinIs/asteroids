# Target S3 bucket name (no s3:// prefix).
BUCKET="your-bucket-name"

# Key prefix within the bucket. Must end with a trailing slash.
PREFIX="path/to/asteroids/"

# AWS CLI profile to use. Can be overridden with: ./deploy.sh --profile <name>
AWS_PROFILE="your-aws-profile"
